services:
  notes:
    build: .
    container_name: homelab-notes
    restart: unless-stopped
    ports:
      - "3010:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=file:../data/notes.db
      - NODE_ENV=production
    env_file:
      - .env
